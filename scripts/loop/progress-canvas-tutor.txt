# Loop Progress Log

Started: 2026-03-28
Feature: canvas-tutor

## Codebase Patterns

- Next.js app lives in `app/`
- For AI tool streaming routes, use `streamText(...).toDataStreamResponse()` with `toolCallStreaming: true`; plain text streams drop tool events, and `maxSteps` only chains when tools return lightweight execute acknowledgements
- Excalidraw must be dynamically imported with `{ ssr: false }`
- Use `convertToExcalidrawElements()` with skeleton objects for programmatic drawing
- Import Excalidraw types from package export paths (`@excalidraw/excalidraw/types`, `@excalidraw/excalidraw/data/transform`)
- When AI adds scene elements, merge with `api.getSceneElements()` first so existing student drawings are preserved
- Keep the canvas command queue as a pure injected executor (`executeCommand` + optional `sleep`) so behavior stays unit-testable
- Treat `SpeechSynthesis` as unreliable I/O: wrap utterances in a cancelable promise with timeout + sync-error handling so narration can never deadlock the queue
- On the client, parse `toDataStreamResponse()` with `processDataStream()` and drive the canvas from streamed `tool_result` acknowledgements so only server-validated commands reach Excalidraw; buffer text deltas into sentence-sized speech queue items for natural narration
- For adaptive tutoring turns, send structured `lessonContext` (`interactionMode`, `currentTopic`, `boardArea`) with the request so the server can force fresh-board-area guidance without trusting ad-hoc text prompts
- Buffer opening narration until the first validated canvas command arrives; it keeps browser speech from outrunning Excalidraw while still allowing text-only turns to speak at end-of-stream
- Keep tutor request retries in a dedicated helper and only retry transient failures (`429`, `503`, network `TypeError`) so the UI recovers gracefully without retrying bad user input

---

## 2026-03-29 - Build command queue with progressive reveal and viewport control

Task ID: 002

- Added `CanvasCommandQueue` with sequential processing, configurable delay, `clear()`, `reset()`, and idle waiting
- Wired the `/canvas` demo to run through the queue and auto-pan the viewport to each newly drawn Excalidraw element
- Added Vitest coverage for queue sequencing/reset behavior and tightened existing Excalidraw skeleton tests so `npm run typecheck` stays green
- **Learnings for future iterations:**
  - Merge AI-authored elements with the live Excalidraw scene instead of a local array to avoid deleting child-authored content
  - Injecting `sleep()` into the queue makes timing logic deterministic to test without fake browser timers

---

## 2026-03-29 - Add voice narration synchronized with canvas drawing

Task ID: 003

- Added a reusable speech player that turns browser `SpeechSynthesis` into a queued, cancelable promise with child-friendly rate/pitch settings
- Interleaved `speak` commands into the canvas demo and added a floating mute toggle that can stop narration mid-run without breaking the draw queue
- Added Vitest coverage for speech playback timing, timeout/cancel behavior, demo sequencing, and control interactions
- **Learnings for future iterations:**
  - Catch synchronous `speechSynthesis.speak()` failures in addition to `onerror` events, otherwise the queue can hang waiting for an utterance that never started
  - Keeping narration sequencing in a small adapter module makes the canvas page stay focused on Excalidraw updates and mute state

---

## 2026-03-29 - Build AI teaching endpoint with streaming tool calls

Task ID: 004

- Added `/api/tutor/teach` using Vercel AI SDK `streamText` with a child-safe whiteboard tutor system prompt and Zod-validated tool definitions
- Enabled streamed tool-call delivery with `toDataStreamResponse()`, `toolCallStreaming: true`, and lightweight execute acknowledgements so the model can chain multiple drawing steps per turn
- Added Vitest coverage for request validation, missing API key behavior, oversized history rejection, and tool schema validation
- **Learnings for future iterations:**
  - Strip client-supplied `system` messages at the route boundary so the server keeps control of the tutor prompt
  - Put conservative caps on streamed conversation history to avoid accidental token blowups before the frontend input layer is finished

---

## 2026-03-29 - Connect frontend to AI streaming endpoint

Task ID: 005

- Replaced the canvas demo controls with a floating tutor prompt overlay, streamed transcript card, loading states, and a missing-key demo fallback
- Wired `/canvas` to `/api/tutor/teach` using manual `fetch()` + AI SDK `processDataStream()` so text becomes queued speech and tool acknowledgements become Excalidraw commands in order
- Added Vitest coverage for the tutor stream parser and the new canvas control surface, plus cleaned the route message mapping so lint/build/typecheck all pass
- **Learnings for future iterations:**
  - `tool_result` stream parts are the safest frontend integration point because they contain the server-validated canvas command returned by each tool `execute()`
  - Buffering text deltas until sentence boundaries keeps browser narration natural; speaking every raw chunk would sound broken and spam the speech queue

---
## 2026-03-29 - Enable student interaction and adaptive teaching

Task ID: 006

- Added turn classification and per-turn lesson context so student answers, confusion, and topic switches send structured guidance to the `/api/tutor/teach` prompt
- Updated the canvas UI to highlight when the tutor asks a question, simplify the visible Excalidraw toolbar to freedraw/text/eraser, and pre-pan to fresh board areas for topic switches or reteaching
- Expanded tests for tutor turn classification, adaptive prompt generation, and the response-focused control state; validated with `cd app && npm test && npm run typecheck && npm run build`
- **Learnings for future iterations:**
  - Layout-sensitive tutoring works better when the client sends explicit board-area context instead of hoping the model infers where to draw next
  - A lightweight UI cue (`Your turn`, answer placeholder, answer button) is enough to make question/response turns feel conversational without adding a chat panel

---

## 2026-03-29 - Polish for demo quality

Task ID: 007

- Buffered opening narration behind the first streamed drawing command so speech no longer gets ahead of the whiteboard, and added smoother viewport timing for the Excalidraw camera
- Added demo-ready resilience: retry/backoff for transient tutor failures, friendly rate-limit/provider errors, offline detection, prompt-length limits, and persisted mute preference
- Refined the floating controls with cleaner status states, character count guidance, and added tests for stream pacing, retry helpers, storage preferences, route validation, and offline UI polish
- **Learnings for future iterations:**
  - Streamed narration feels much more natural when the client waits for the first validated visual command before speaking the opening sentence
  - Shared client/server limits (like prompt length) prevent the UI and API from drifting into inconsistent behavior during demos

---
