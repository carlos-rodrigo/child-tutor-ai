# Loop Progress Log

Started: 2026-03-27
Feature: child-tutor-ai

## Codebase Patterns

- Next.js 16 lint config in this app rejects synchronous `setState` inside effects; derive render-only state from props/turn data and keep async state transitions inside handlers.
- App code lives under `app/` (Next.js app router project nested inside repo root).
- Keep tutor/canvas contracts centralized in `app/lib/tutor-types.ts`.
- Keep lesson content isolated from rendering so future topics can swap in cleanly.
- Maintain AI-SDK-compatible backend shape even when using fallbacks.
- Apply structural state (canvas scene, turn data) from response headers immediately on headers-received, stream speech separately — never defer canvas update until after speech streaming ends.
- Wrap JSON.parse of `x-tutor-turn` header in try-catch — proxy corruption can cause malformed JSON.
- New lesson steps must be added to `progressMap` in `page.tsx` and routed in `pickNextTurn` in `respond/route.ts`.

---

## 2026-03-27 - Refine visual design direction and lesson shell

Task ID: 001

- Implemented a tighter lesson-first shell: compact header, oversized canvas board column, and integrated tutor/answer interaction rail.
- Reworked `app/app/page.tsx` layout to keep controls in one place and added a clear “Start lesson” entry flow before session creation.
- Rewrote `app/app/globals.css` to establish the new visual system (glass panels, notebook board styling, integrated answer cards, responsive layout).
- Ran validation with `cd app && npm run lint && npm run build`.
- Oracle sub-agent review could not run in this environment (missing provider API key), so a manual quality/security/performance/testing review was performed before finalizing.
- **Learnings for future iterations:**
  - Next.js 16 hook linting in this repo is strict about state updates inside effects; keep rendering transforms pure (e.g., `buildScene`) and use handlers for state transitions.
  - Start/restart flow should remain explicit in the UI to keep shell state deterministic and avoid mount-side effects.

---

## 2026-03-27 - Improve canvas engine and visual primitives

Task ID: 002

- Enhanced CSS for all canvas primitives in `app/app/globals.css`
- Added smooth transition on segment fill changes (0.38s ease on `background`)
- Added `.filled` CSS class with `::after` highlight sheen on filled segments
- Strengthened segment dividers from 0.16 to 0.22 opacity, 2.5px border
- Added `highlightPulse` animation (opacity only, no box-shadow for perf)
- Added `pointerBounce` animation via `transform: translateY` (GPU-accelerated)
- Added chalk-like `text-shadow` to `.board-text`
- Refined `.rect` shadow with inset highlight ring
- Added horizontal split rendering in `CanvasView` — `flexDirection: "column"` + `height: N%` per segment
- **Learnings for future iterations:**
  - Never animate `box-shadow` in keyframes — it causes repaints; use opacity/transform only
  - Horizontal flex splits need `flexDirection: "column"` + `height` sizing (not `width`)
  - `.filled` class pattern with `::after` is a clean way to add state-dependent decorations

---

## 2026-03-27 - Add streaming tutor speech rendering in UI

Task ID: 003

- Changed `app/app/api/tutor/respond/route.ts`: replaced `result.toDataStreamResponse()` with a native `ReadableStream` that pipes `result.textStream` as raw `text/plain` bytes — fixes garbled SSE-format output on the client.
- Changed `app/app/page.tsx`: added `speechRef` (useRef) to hold final speech text; removed `streamedSpeech` from `useEffect` dependency array so `speak()` only fires once per turn (not on each streaming chunk); added progressive `setStreamedSpeech(text)` inside the read loop so speech appears in the UI as it streams in.
- Fallback JSON path (no API key) unchanged in behavior; updated to set `speechRef.current` for consistency.
- Build passes: `cd app && npm run build` ✅
- **Learnings for future iterations:**
  - AI SDK `toDataStreamResponse()` sends SSE-format bytes (`0:"chunk"`), not raw text — always use `result.textStream` for plain-text streaming to non-SDK clients.
  - Decouple display state from speech trigger: use a `useRef` to hold the final text and remove the display-state from effect deps to avoid repeated `speak()` calls per chunk.

---

## 2026-03-27 - Improve lesson flow and state transitions

Task ID: 004

- Added `getIncorrectQuarterTurn()` in `fractions-lesson.ts` — warm retry turn with highlight and hint for the quarters question so the incorrect branch feels intentional, not like a no-op loop
- Updated `pickNextTurn` in `respond/route.ts` to route `correct + wrong` → `incorrect-quarter` step (and `incorrect-quarter + wrong` → another retry)
- Moved `setTurn(nextTurn)` + `setSession(...)` up to immediately after response headers arrive in `page.tsx` — canvas and question now update instantly, speech streams into the already-rendered new scene
- Added `selectedAnswerId` state to show which choice button was tapped (● icon, scale-down, opacity) during the evaluating phase
- Added `evaluating` derived state (`isLoading && selectedAnswerId !== null`) to show "Thinking…" in the speech card with a fade pulse animation while awaiting the server
- Added `.thinkingPulse` CSS animation and `.speech-card.thinking` class (opacity only — GPU-safe)
- Added `.choice-card.selected` CSS class with scale-down + inset border ring
- Updated progress map to include `"incorrect-quarter": 75` for accurate lesson progress
- Wrapped `JSON.parse(headerTurn)` in try-catch for robustness against proxy header corruption
- Build passes: `cd app && npm run build` ✅
- **Learnings for future iterations:**
  - Apply canvas/structural state from response headers immediately; stream speech text separately — never defer DOM structure until after streaming ends
  - `evaluating && !streamedSpeech` accurately gates a thinking placeholder with no explicit clear logic needed
  - Always wrap JSON.parse of response headers in try-catch — proxies can corrupt them

---

## 2026-03-27 - Add voice input beta with safe fallback

Task ID: 005

- Added `VoiceState` type (`idle | listening | unsupported | denied`) and `voiceState` state
- Added `getSpeechRecognition()` helper with full local TypeScript interface declarations (ISpeechRecognition, SpeechRecognitionCtor) — no ts-ignore, no `as any`
- Added `startVoiceInput()` using Web Speech API: sets `listening`, fires `answer()` on match, handles `not-allowed` → `denied`, all other errors → silent `idle` fallback
- Transcript matched against choice labels with empty-string guard (fixed review bug)
- `onend` functional updater preserves sticky failure states
- Mic button renders below choices only when lesson is active; choice buttons always visible
- Added CSS: `.voice-input-row`, `.mic-button`, `.mic-button.listening` (pulse ring), `.beta-badge`, `.voice-hint`
- Build passes: `cd app && npm run build` ✅
- **Learnings for future iterations:**
  - Web Speech API types not in standard TS lib — use local interface declarations with a ctor type alias
  - `label.includes("")` is always true — always guard `if (!transcript) return` before symmetric includes matching
  - SpeechRecognition `onend` always fires after `onresult`/`onerror` — use functional state updater to avoid resetting sticky states

---

---

## 2026-03-27 - Polish completion state and replay flow

Task ID: 006

- Replaced the generic `completion-card` with a dedicated `completion-panel` component that renders only when `turn?.stepId === "completed"`
- Added bouncing 🏆 trophy (`trophyBounce` keyframes on transform only), "Lesson complete!" heading, two-row fraction summary (`½` / `¼`), and a styled `replay-cta` button with mint gradient
- Added `completionEntry` keyframe (transform + opacity, GPU-safe) for smooth entry animation
- Fixed `startSession()` voice state reset: `(s === "unsupported" ? "unsupported" : "idle")` — clears `listening` and `denied`, keeps environmental `unsupported` sticky
- Added `will-change: transform` to `.replay-cta` to hint the compositor before hover transforms
- Simplified pre-session fallback: `completion-card` now always shows "Ready when you are." (removed the session-conditional "Nice work, explorer." branch — that state is now fully handled by `completion-panel`)
- Files changed: `app/app/page.tsx`, `app/app/globals.css`
- **Learnings for future iterations:**
  - In a finite choice-based lesson, `isCompleted` guard before `choices.length` gives clean, distinct state branches
  - Voice state reset pattern: `(s === "unsupported" ? "unsupported" : "idle")`
  - `will-change: transform` on interactive buttons with hover transforms avoids compositor surprises

---
